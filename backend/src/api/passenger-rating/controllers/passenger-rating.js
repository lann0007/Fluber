'use strict';

/**
 * passenger-rating controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::passenger-rating.passenger-rating');
